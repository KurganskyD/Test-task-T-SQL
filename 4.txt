/*я решил разделить задание 4-6, так как по сути автомат сначал проверит если ли напиток и только потом будет делать другие действи€. ѕоэтому в этой
процедуре будет только проверка есть ли напиток в наличии. ≈сли нет, то на выходе 1, иначе на выходе цена продукта, котора€ будет входным параметром
дл€ следующей процедуры. ≈сли же без самоде€тельности то € бы выполнил все проверки через IF (условие) and (условие)... в процедуре задани€ 5*/
CREATE PROCEDURE [dbo].[DrinkIsNullOrNot] @product varchar(40) --ќпределе€м входной параматер ¬ид продукта
AS
DECLARE @price INT = (SELECT price FROM DRINKS WHERE name = @product) --ќпредел€м переменную и присваиваем ей значение цены продукты, что будет входным параметром след.процедуры приема денег
IF (SELECT number FROM DRINKS WHERE name = @product) != 0 --ѕровер€ем есть ли напиток в автомате
	Return @price --≈сли есть, то возвращаем цену и автомат переходит к режиму приема монет, при каждом опускание которых будет вызыватьс€ след. процедура
Else Return 1 --»наче возвращаем 1 и выводим информацию клиенту на монитор, что напитка нет